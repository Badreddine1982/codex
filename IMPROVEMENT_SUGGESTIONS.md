# اقتراحات عملية لتطوير Codex CLI
> مبنيّة على تحليل فعلي للكود في 2026-08-24 — كل رقم أدناه قابل للتحقق.

---

## 🔴 الأولوية العالية (أثر كبير، مخاطرة منخفضة)

### 1. تقسيم الملفات العملاقة (God Files)
المستودع يفرض على نفسه قاعدة "الوحدة تحت 500 سطر" في `AGENTS.md`، لكن الواقع:

| الملف | الحجم | نسبة التجاوز |
|---|---|---|
| `tui/src/bottom_pane/chat_composer.rs` | **12,816 سطر** | ×25 |
| `tui/src/resume_picker.rs` | 6,903 سطر | ×14 |
| `protocol/src/protocol.rs` | 6,011 سطر (183 نوع عام!) | ×12 |
| `app-server/src/request_processors/thread_processor.rs` | 5,772 سطر | ×11 |
| `state/src/runtime/memories.rs` | 5,445 سطر | ×11 |
| `cli/src/main.rs` | 4,551 سطر | ×9 |

**خطوات عملية:**
- ابدأ بـ `protocol.rs`: انقل الأنواع إلى وحدات فرعية (`protocol/src/items.rs`, `events.rs`, `permissions.rs`...) وأعد التصدير من الجذر — تغيير ميكانيكي لا يكسر الـ API العام.
- لـ `chat_composer.rs`: استخرج حالات الآلة (IME، الإكمال، سجل الأوامر) كلٌ في وحدة.
- أضف فحص CI يرفض أي ملف يتجاوز 800 سطر (القاعدة موجودة لكن لا يوجد باب يفرضها): سكربت Python صغير في `repo-checks.yml`.

### 2. حماية مُحلّل الـ Patches عبر Fuzzing
`apply-patch` يُحلّل نصوصاً **مولّدة من نموذج لغوي** (مدخلات غير موثوقة بطبيعتها)، ولا يوجد **أي هدف fuzzing في المستودع كله**:
```
find . -name "fuzz" -type d  →  (لا شيء)
```
كما أنه يحتوي 77 استدعاء `unwrap()` في كود الإنتاج.

**خطوات عملية:**
```bash
cargo new --lib codex-rs/apply-patch-fuzz
# أضف cargo-fuzz target يستدعي apply_patch() على مدخلات عشوائية
cargo +nightly fuzz run apply_patch -- -max_total_time=300
```
- شغّله 10 دقائق محلياً؛ أي panic يعني حماية من crash حقيقي في الإنتاج.
- أضف corpus من الـ patches الحقيقية في `apply-patch/tests/` كبذور.

### 3. جدولة سداد الدين التقني الموثّق (344 TODO/FIXME)
تكرارات مكشوفة تدل على دين بنيوي وليس ملاحظات متفرقة:
- **7+ نسخ** من نفس الـ TODO في `app-server-protocol/v2/permissions.rs`: «Remove this conversion once core permission paths use PathUri»
- نفس النمط يتكرر في `app-server/src/bespoke_event_handling.rs` و `command_exec.rs`

**خطوات عملية:**
- أنشئ Epic واحداً: "توحيد المسارات على `PathUri` في النواة" — يحذف ~10 TODOs دفعة واحدة ويقلّل طبقات تحويل عرضة للأخطاء.
- أضف أمر `just todos` (awk بسيط) يطبع التقرير شهرياً لقياس الاتجاه.

---

## 🟡 الأولوية المتوسطة (قيمة واضحة، جهد متوسط)

### 4. معالجة `unwrap()` في المسارات الحرجة (1,382 حالة)
التوزيع يكشف بؤراً مركّزة — الأهم أمنياً:

| Crate | عدد `unwrap()` | الخطورة |
|---|---|---|
| `network-proxy` (5 ملفات) | ~200 | 🔥 عالية — مكوّن شبكي |
| `tui/src/resume_picker.rs` | 95 | متوسطة — مسار استئناف الجلسات |
| `apply-patch/src/lib.rs` | 77 | 🔥 عالية — مدخلات النموذج |
| `file-search/src/lib.rs` | 59 | متوسطة |

**خطوات عملية:**
- `cargo clippy -- -W clippy::unwrap` على `network-proxy` فقط، وعالجها بـ `anyhow::Context` — نطاق مغلق يمكن إنجازه في PR واحد.
- امنع التراجع: أضف `#![cfg_attr(not(test), deny(clippy::unwrap))]` داخل الـ crates المنظّفة فقط، واحدة تلو الأخرى.

### 5. معايير أداء (Benchmarks) للمسارات الساخنة
يوجد بنشمارك **واحد فقط** في المستودع (`utils/image` بمكتبة divan) رغم وجود مسارات ساخنة واضحة: تحليل الـ patches، البحث في الملفات، وعرض الـ TUI (69 ألف سطر).

**خطوات عملية:**
```toml
# apply-patch/Cargo.toml
[[bench]]
name = "parse"
harness = false
```
- قِس `apply_patch()` على patches حقيقية بأحجام 1KB/100KB/10MB — يحمي من انحدار الأداء الصامت في كل PR.
- بنشمارك TUI: قِس زمن رسم 1000 إطار عبر `cargo bench` في `tui`.

### 6. توحيد إصدارات الاعتماديات (1,353 اعتمادية)
نسخ مكررة تزيد زمن الترجمة وحجم الـ binary:
- `windows-sys` ×5، `hashbrown` ×5، `windows_x86_64_*` ×3

**خطوات عملية:**
```bash
cargo update --precise   # رفع النسخ القديمة لتتوحد
cargo tree -d            # قائمة التكرارات مع من يطلبها
```
- أضف فحصاً في `cargo-deny.yml` (الموجود أصلاً): قاعدة `duplicates = "deny"` مع استثناءات موثقة.

---

## 🟢 الأولوية الاستراتيجية (أثر طويل المدى)

### 7. سدّ فجوة النضج بين SDK بايثون وTypeScript
- Python SDK: ~13,457 سطر | TypeScript SDK: ~1,034 سطر (9 ملفات فقط)
- الـ TS SDK لا يملك حتى نصف ميزات نظيره (مثل إدارة MCP المتقدمة).

**خطوات عملية:** انقل اختبارات المطابقة (conformance) لتصبح مصدر حقيقة مشتركاً — `mcpConformance.test.ts` موجود بالفعل؛ عمّمه على كل واجهة `Thread` بحيث تُشتغل نفس حالات الاختبار على SDK بايثون (عبر pytest parametrize) وتكشف تلقائياً أي فجوة ميزات.

### 8. دعم تعدد لغات الواجهة (i18n)
لا توجد أي بنية تعريب في الـ TUI — كل النصوص hardcoded بالإنجليزية. المشروع عالمي ومستخدموه غير ناطقين بالإنجليزية بالضرورة.

**خطوات عملية:**
- انقل نصوص الواجهة إلى `tui/src/strings.rs` (فلتر بسيط ثمّ flux.rs).
- اعتمد نمط `fluent` (mozilla) أو حتى ملفات JSON بسيطة: `tui/locales/ar.json`.
- ابدأ بلغة واحدة (مثلاً العربية أو اليابانية) كمشروع إثبات.

### 9. CHANGELOG محلي قابل للبحث
`CHANGELOG.md` الحالي = سطر واحد يحيل إلى GitHub Releases (لا يُنصّف offline ولا يُربط بالـ commits).

**خطوات عملية:** ولّده آلياً من conventional commits عبر `git-cliff` في `rust-release-prepare.yml` — لا جهد يدوي، ويبقى الملف في المستودع.

### 10. تقرير صحة المستودع الآلي (Repo Health Dashboard)
كل الاقتراحات أعلاه تحتاج قياساً دورياً كي لا تعود المشاكل.

**خطوات عملية:** سكربت واحد يُضاف لـ `repo-checks.yml` يطبع (كتعليق في PR أو artifact):
- عدد الملفات >800 سطر (اليوم: ~15+ ملف)
- عدد `unwrap()` الإنتاجية (اليوم: 1,382)
- عدد TODO/FIXME (اليوم: 344)
- عدد النسخ المكررة من الاعتماديات (اليوم: 13+)

هذا يحوّل المراجعة من "مرة واحدة" إلى **منحنى قابل للتتبع**.

---

## 📋 خارطة طريق مقترحة (ترتيب التنفيذ)

| المرحلة | المدة التقديرية | المحتوى | الحالة |
|---|---|---|---|
| 1 | أسبوع | Fuzzing لـ apply-patch (#2) + تنظيف unwrap في network-proxy (#4) | ⏳ |
| 2 | أسبوعان | بوابات CI: حد حجم الملف + تقرير الصحة (#1، #10) | ✅ **مُنفّذ ومُختبَر** |
| 3 | مستمر | Epic الـ PathUri (#3) — الخطة جاهزة في `PATHURI_MIGRATION.md` + بوابة TODO مفعّلة؛ التنفيذ يتطلب بيئة Rust | 🔶 الخطة+ratchet جاهزان |
| 3 | شهر | تقسيم protocol.rs و chat_composer.rs (#1) + بنشماركات (#5) | ⏳ |
| 4 | مستمر | i18n (#8) + سد فجوة TS SDK (#7) + CHANGELOG آلي (#9) | ⏳ |

---

## ✅ ما تم تنفيذه (2026-08-24)

### بوابة صحة المستودع (البند #1 + #10)
- **`scripts/check_repo_health.py`** — بوابة ratchet تفرض حد 800 سطر (قاعدة AGENTS.md):
  - ملف جديد ضخم → **فشل فوري** في CI ✔ (مُختبَر)
  - ملف موجود تجاوز baselineه → **فشل** ✔ (مُختبَر)
  - تقليص/حذف ملف → نجاح + اقتراح تحديث baseline ✔ (مُختبَر)
  - تستبعد الاختبارات وvendor والكود المولّد آلياً
  - وضع `--metrics`: لوحة مقاييس (1,179 unwrap إنتاجي، 229 TODO، **95** اعتمادية مكررة)
- **`scripts/repo-health-baseline.json`** — لقطة الدين الحالي (239 ملفاً) — تنخفض فقط
- **`scripts/test_check_repo_health.py`** — 9 اختبارات unittest (كلها ناجحة)
- **الدمج**: وصفة `just repo-health` في justfile + خطوتان في `repo-checks.yml`

> ملاحظة: أرقام المقاييس هنا أدق من الجدول أعلاه لأنها تستبعد الاختبارات والكود المولّد.

### Epic ترحيل PathUri + بوابة TODO (البند #3)
- **`PATHURI_MIGRATION.md`** — خطة تنفيذية كاملة لسداد الدين: جرد آلي دقيق كشف
  **42 موقع TODO(anp)** في 22 ملفاً عبر 10 crates (أكبر مما قدّر التحليل الأول:
  21 موقعاً) — مقسمة إلى 5 مراحل بترتيب تبعيات صحيح، مع معايير قبول قابلة للفحص
  لكل مرحلة. تنفيذ المرحلة 3 وحدها يحذف 7 كتل تحويل متطابقة.
- **بوابة TODO ratchet** في `check_repo_health.py`: العدد الإنتاجي (229) صار سقفاً
  في CI — أي TODO جديد **يفشل الـ build**، والانخفاض فقط مسموح ✔ (مُختبَر: إضافة
  TODO واحد → exit 1)
- **`just todos`** — تقرير مجمّع حسب المالك: (anp) يملك 57 من أصل 223 TODO إنتاجية.
- اختبارات الوحدة ارتفعت من 9 إلى **15** — كلها ناجحة.
